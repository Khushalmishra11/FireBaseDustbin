{
  "name": "firebase-dustbin-level-notifier",
  "version": "1.0.0",
  "description": "Cloud Functions to monitor dustbin fill level changes and send notifications",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  },
  "private": true
}
